import Publisher from "../Publisher";

const potMaterialChange = new Publisher();
const potSoildChange = new Publisher();

const handlePotMaterialChange = (event) => {
  // data-id = ceramic
  potChange.publish(data);
}

const handlePotColor = (event) => {
// data-id = purple
potChange.publish(data);
}

const handlePotDecoration = (event) => {
// data-id = 
potChange.publish(data);
}

export { potMaterialChange, potSoildChange };

// atachar evento a input material handlePotMaterialChange()
